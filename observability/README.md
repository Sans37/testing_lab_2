# Observability Outputs

This folder stores tracing outputs when `ENABLE_TRACING=true`.
- `otel_traces.jsonl` (benchmark / local runs)
- `otel_traces_ci.jsonl` (CI/CD runs)
- `otel_marker_ci.txt` (optional marker that tracing was enabled)
